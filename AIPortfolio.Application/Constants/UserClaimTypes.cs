namespace AIPortfolio.Application.Constants;

public static class UserClaimTypes
{
    public const string Id = nameof(Id);
    public const string FullName = nameof(FullName);
    public const string Email = nameof(Email);
}